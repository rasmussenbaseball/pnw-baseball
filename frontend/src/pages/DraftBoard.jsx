import Placeholder from './Placeholder'
export default function DraftBoard({ year }) {
  return <Placeholder title={`MLB Draft Board '${year}`} description={`PNW players projected for the 20${year} MLB Draft. Coming soon!`} />
}
